apiVersion: v1
kind: Service
metadata:
  name: flask-monitoring
spec:
  selector:
    app: flask-monitoring
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP









