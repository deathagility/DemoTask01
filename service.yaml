apiVersion: v1
kind: Service
metadata:
  name: flask-monitoring-01
spec:
  selector:
    app: flask-monitoring-01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP









