apiVersion: v1
kind: Service
metadata:
  name: flask-monitoring-11
spec:
  selector:
    app: flask-monitoring-11
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP









